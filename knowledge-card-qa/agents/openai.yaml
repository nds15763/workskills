interface:
  display_name: "Knowledge Card QA"
  short_description: "判断是否落卡并生成快问快答"
  default_prompt: "Use $knowledge-card-qa to decide whether this project knowledge needs a card, then write the card if it does."
policy:
  allow_implicit_invocation: true
